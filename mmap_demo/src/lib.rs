use nix::sys::socket::AddressFamily;

// hCS

oct bridge 
handle(base_address: u64){
    address -> virtual memory Address
    ?baseaddress
}
//mmap only called once
mmaper
impl mmaper{
    fn map(){}
    fn get_baseaddress()-> u64{}
}